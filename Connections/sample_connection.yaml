# Sample connection — uses the built-in mock cube so you can test immediately
# Replace with your real Power BI connection string when ready

connection_string: "MOCK://contoso"
description: "Mock Contoso Sales cube (works out of the box, no Power BI needed)"
command_timeout_seconds: 300
